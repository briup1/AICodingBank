# 工程结构与协作地图

> 状态：草案（待人工确认）
> 目标：让读者能判断代码在哪里、模块如何协作、一次需求应修改哪些位置。按项目实际子系统增删章节。

## 1. 系统总览

```text
{从真实运行入口提取：用户/调用方 → 前端/API/CLI → 后端/服务 → 存储或外部系统 → 返回路径}
```

| 运行单元 | 技术栈 | 启动入口 | 对外边界 | 主要职责 | 来源 |
|----------|--------|----------|----------|----------|------|
| {按项目填写} | {语言/框架} | {文件/命令} | {HTTP/RPC/事件/库} | {一句话} | {配置/路径} |

## 2. 仓库导航

| 路径/模块 | 内部职责 | 主要入口 | 调用方 | 允许依赖 | 不应承载 | 测试位置 |
|-----------|----------|----------|--------|----------|----------|----------|
| {生产模块逐项填写} | {按项目填写} | {文件/符号} | {模块} | {模块} | {职责边界} | {路径} |

生产模块必须全量覆盖；生成物、vendor、样例和测试辅助可分组说明。复杂子系统分别增加“前端内部结构”“后端内部结构”“worker/CLI 内部结构”等小节，不把整个目录压缩成一句话。

## 3. 构建依赖与运行时协作

### 3.1 构建依赖

```text
{从 import、workspace、注册表和生成关系提取；箭头含义必须注明}
```

### 3.2 运行时链路

对每条核心链路写明入口、跨模块调用、状态变化、外部交互、返回路径和错误落点：

```text
{调用方}
  → {模块/函数}
  → {接口/消息}
  → {处理方}
  → {状态/存储/外部系统}
  → {响应/通知}
  → {最终消费者}
```

## 4. 子系统内部结构

按项目实际子系统逐个生成。每节至少回答：

- 入口和装配点在哪里；
- 页面/组件/handler/service/store/adapter 等真实职责如何分工；
- 新增行为、状态、接口或基础设施能力分别落到哪里；
- 哪些状态或语义只能由指定模块拥有；
- 如何复用现有扩展点而不是另建平行机制。

## 5. 变更落点地图

| 需求类型 | 调用方/界面落点 | 核心逻辑落点 | 提供方/基础设施落点 | 契约与状态文档 | 必要验证 |
|----------|-----------------|--------------|-------------------|----------------|----------|
| {项目常见需求逐项填写} | {路径} | {路径} | {路径或 N/A} | {wiki/权威来源} | {测试/命令} |

至少覆盖一个跨模块功能、一个接口/协议变更和一个状态/错误修复；项目不存在某类任务时写明 N/A。

## 6. 架构约束与默认实践

| ID | 陈述 | 类型 | 范围 | 证据 | 反例/偏离条件 | 验证 |
|----|------|------|------|------|---------------|------|
| ARCH-{序号} | {具体约束或默认落点} | 已确认规范/默认实践/事实/技术债/提议 | {模块} | {路径:行号} | {例外或无} | {自动/人工} |

## 7. 扩展点与例外

| 扩展点/例外 | 用途 | 接入步骤 | 边界 | 当前使用者 | 证据 |
|-------------|------|----------|------|------------|------|
| {按项目填写} | {按项目填写} | {最小步骤} | {不得越过的职责} | {路径} | {路径:行号} |

## 8. 架构解释（可选）

记录项目自述名称、候选解释、支持证据、反证、适用范围和置信度。证据不足时使用“项目自有架构”“混合架构”或“当前无法命名”，不因目录名或框架强贴标准模式。

## 9. 完整性记录

- 生产运行单元：{发现数}；已覆盖：{数量}；排除：{数量与理由}。
- 生产模块：{发现数}；已覆盖：{数量}；排除：{数量与理由}。
- 核心运行时链路：{发现数}；已覆盖：{数量}。
- 验证基线：{branch@commit + 时间}。
