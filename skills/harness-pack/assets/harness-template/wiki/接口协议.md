# 接口与集成契约

> 状态：草案（待人工确认）
> 目标：只读本文及其子文档即可构造合法调用，并理解成功、失败、安全、副作用和状态影响。接口较多时按类别拆文件，本文件改作目录，但每个条目仍须有完整契约。

## 1. 契约范围

| 类别 | 方向 | 权威来源 | 发现数 | 已文档化 | 排除数 | 版本/基线 |
|------|------|----------|--------|------------|--------|-----------|
| {HTTP/RPC/Event/CLI/Library/其他} | {调用方 → 提供方} | {schema/正式文档/代码} | {数量} | {数量} | {数量+理由} | {版本/commit} |

生成代码或大型外部协议只完整记录项目实际暴露或消费的子集，并链接权威全集。

## 2. 快速目录

| 契约 ID | Method/Path/Name | 用途 | 调用方 | 提供方 | 详细章节/文件 | 状态 |
|---------|------------------|------|--------|--------|---------------|------|
| API-{序号} | {完整名称} | {一句话} | {模块} | {模块/外部系统} | {锚点或文件} | proposed/current/deprecated |

## 3. 逐项契约

以下章节对目录中的每一项重复，禁止用“同上”“见实现”或仅链接代替字段说明。

### {API-ID} `{METHOD /path` 或 `message.name`}

**用途与方向**

- 用途：{调用目的}。
- 方向：{调用方} → {提供方}。
- 调用位置：{文件:符号}。
- 实现位置：{文件:符号}。
- 版本/状态：{current/proposed/deprecated + 兼容策略}。

**鉴权与前置条件**

- {Session/Cookie/Token/Origin/权限/连接状态/资源归属；不适用写 N/A}。

**请求或事件**

| 位置 | 字段 | 类型 | 必填 | 约束与语义 | 来源 |
|------|------|------|------|------------|------|
| path/query/header/body/payload | {字段} | {准确类型} | 是/否 | {范围、格式、默认值、关联关系} | {schema/代码} |

**成功响应或结果**

- 状态码/结果类型：{按项目填写}。

| 字段 | 类型 | 必有 | 语义 | 来源 |
|------|------|------|------|------|
| {字段} | {类型} | 是/否 | {含义} | {schema/代码} |

**错误与关闭语义**

| 状态码/错误/关闭码 | 触发条件 | 外部内容 | 调用方处理 | 来源 |
|--------------------|----------|----------|------------|------|
| {按项目填写} | {条件} | {脱敏后的结构} | {重试/提示/失效/终止} | {代码/测试} |

**副作用与时序**

- 数据/状态变化：{按项目填写或 N/A}。
- 幂等、重复、顺序、并发或 revision：{按项目填写或 N/A}。
- 兼容与迁移：{按项目填写或 N/A}。

**最小合法示例**

```text
{使用虚构、非敏感值给出请求/事件和成功结果；不能确认的字段不得编造}
```

**验证**

- 契约来源：{路径:行号}。
- 实现来源：{路径:行号}。
- 测试/fixture：{路径:测试名}。

## 4. 跨契约语义

仅记录多个接口共同遵循的真实机制：错误包装、分页、版本、关联 ID、顺序、重试、鉴权、速率或兼容规则。单个接口的例外必须回写该接口章节。

## 5. 维护

- 新增、删除或修改公开入口时，同步目录、逐项契约和完整性计数。
- 权威来源与本文冲突时以权威来源为准，并把本文视为必须修复的过期派生视图。
- 敏感值只用虚构示例；未实现设计标 proposed。
