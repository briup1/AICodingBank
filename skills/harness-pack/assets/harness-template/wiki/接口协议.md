# 接口协议

> 状态：草案（待人工确认）
> 定位：不读实现即可构造合法调用——本文是写调用代码的**输入规格**，也是改契约前的**雷达**（每个端点都有文档之外的消费者）。
> 激活入口：CLAUDE.md 场景触发行；`skills/需求分析/SKILL.md` Step 4；`skills/编码实现/SKILL.md` 前置条件。
> 权威源是 {schema 文件/路由代码}；与本文冲突时以权威源为准，本文是必须修复的过期派生视图。
> 验证基线：{branch@commit + 时间}

## 1. 通用约定（全项目只写一次，端点不复述）

- **响应结构**：{envelope 形状，明确错误时业务码出现在哪一层}；错误响应示例：{一个虚构示例}
- **错误码表**：| 码 | 含义 | 典型触发 | 调用方处理 |
- **鉴权方式**：{机制、凭证位置、401/403 语义}
- **分页约定**：{参数名、响应元数据字段}
- **关联 ID**：{trace/request id 的传递与返回}

## 2. 契约范围与完整性

| 类别 | 方向 | 权威来源 | 发现数 | 已文档化 | 排除数+理由 | 基线 |
|------|------|----------|--------|----------|--------------|------|
| {HTTP/RPC/Event/CLI/…} | {调用方→提供方} | {schema/代码} | {} | {} | {} | {} |

完整性闭合：发现数 = 文档化数 + 排除数。生成代码或大型外部协议只记项目实际暴露/消费的子集，链接权威全集。

## 3. 端点清单

### 3.0 快速目录

| ID | Method Path | 用途 | 调用方 | 提供方 | 状态 |
|----|-------------|------|--------|--------|------|

状态取值：`current` / `proposed`（未实现）/ `deprecated`（注明兼容策略）。接口很多时按类别拆文件，本文件改作目录，但每个条目仍须完整契约。

**编号规则（跨文档稳定主键）**：首版按 method+path 字典序分配 API-ID；再版时已有端点编号保持不变，新增端点取当前最大序数 +1，删除的编号不再复用——数据模型、业务模型等文档按 ID 引用，重排编号会静默打断全部交叉引用。

### {域分组，如：商品 / 订单}

#### {API-序号} {METHOD} /path —— 一句话职责

- 鉴权：{N/A 写明理由}；调用方：{模块/页面/外部系统——改契约前的雷达}
- 幂等与副作用：{能否安全重试；数据/状态变化；N/A 写明}
- 错误：引用通用错误码表 + 本端点特有项

**请求参数**

| 位置(path/query/header/body) | 字段 | 类型 | 必填 | 约束与说明（范围、默认值、枚举全集） |
|------------------------------|------|------|------|--------------------------------------|

**响应参数**

| 字段 | 类型 | 必有 | 说明（语义、枚举全集） |
|------|------|------|------------------------|

每域给一个最小合法请求/响应示例（虚构非敏感值）；字段不能确认时标 proposed，不编造。条目内禁止"同上""见实现"代替字段说明。

## 4. 跨契约语义

仅记录多个接口共同遵循的真实机制：错误包装、顺序、重试、兼容规则等。单个接口的例外回写该端点条目，不在此复述。

## 5. 维护

- 同步时机归 `rules/开发流程规范.md` 与 harness-sync 兜底，本文不自管；
- 权威源与本文冲突时以权威源为准，并把差异视为必须修复项；
- 敏感值只用虚构示例；未实现设计标 proposed。
