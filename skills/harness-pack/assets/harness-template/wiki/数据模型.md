# 数据与状态模型

> 状态：草案（待人工确认）
> 定位：改库/改状态前的雷达——什么对象已存在、归谁管、状态怎么流转；字段级陷阱（单位、软删除、快照）的成文处。
> 激活入口：CLAUDE.md 场景触发行（表结构与状态→本文）；`skills/需求分析/SKILL.md` Step 4。
> 权威源是 {schema/迁移代码/ORM 模型}；与本文冲突时以权威源为准，本文是必须修复的过期派生视图。
> 验证基线：{branch@commit + 时间}
> 数据库只是存储方式之一；内存、文件、浏览器状态、缓存、消息、远端真相源和本地投影都应按实际情况记录。

## 1. 所有权与存储地图

| 数据/状态 | 权威所有者 | 存储/投影位置 | 创建者 | 读取者 | 修改者 | 生命周期 | 隔离/权限 | 来源 | 状态 |
|-----------|------------|---------------|--------|--------|--------|----------|-----------|------|------|
| {关键对象逐项填写} | {模块/外部系统} | {DB/文件/内存/浏览器/远端} | {模块} | {模块} | {模块} | {创建到清理} | {用户/租户/路径} | {schema/代码} | proposed/current/deprecated |

本地投影不得被写成权威来源；同一对象存在多份表示时说明同步方向和失效条件。

## 2. 对象结构与约束

### {对象名称}

- **用途**：{按项目填写}。**权威定义**：{schema/迁移/ORM}。**所有者**：{模块/系统}。
- **关键模式**：{快照 / 引用 / 累加 / 软删除——一句话点明该对象的特殊语义；无则省略}

| 字段 | 类型 | 必填 | 默认值 | 约束与说明（唯一、索引、单位、枚举、敏感级别） | 来源 |
|------|------|------|--------|------------------------------------------------|------|
| {字段} | {类型} | 是/否 | {值或无} | {唯一性/引用/范围/单位（如分）/枚举/公开·内部·敏感} | {路径:行号} |

- **快照字段标注**：存的是"某时点存档"还是"引用"必须写明——快照不可随源数据更新，新代码禁止把快照改成实时 join。
- **索引与唯一约束**：{列出非主键索引与 UNIQUE——AI 加查询前先核对，避免全表扫或建重复索引}
- **软删除**：{查询默认是否过滤已删行；恢复方式；N/A 写明}
- **生命周期**：`{创建} → {读取/更新} → {失效/删除/归档}`
- **一致性与并发**：revision、锁、事务、幂等、乱序或冲突处理：{按项目填写或 N/A}；部分失败与恢复：{按项目填写或 N/A}
- **读写入口**：创建/更新/读取/删除各经哪个接口或方法（接口 ID + 代码位置）；测试：{路径/测试名}

## 3. 状态机与枚举

### {状态对象}

| 当前状态 | 事件/动作 | 下一状态 | 守卫条件 | 副作用 | 失败处理 | 来源 |
|----------|-----------|----------|----------|--------|----------|------|
| {逐条填写} | {事件} | {状态} | {条件} | {影响} | {行为} | {代码/测试} |

| 枚举值 | wire/存储值 | 含义 | 产生方 | 消费方 | 未知值处理 | 来源 |
|--------|-------------|------|--------|--------|------------|------|
| {每个实际值} | {原值} | {含义} | {模块} | {模块} | {忽略/错误/兼容} | {代码} |

状态和枚举属于有限集合，实际消费子集必须全量核对；大型生成全集只需链接权威定义。

> **边界**：状态与枚举的权威只在本文档；`领域术语.md` 与 `业务模型.md` 只留链接、不复述枚举值——同一枚举三处书写是漂移温床。

## 4. 持久化、迁移与清理

按实际机制记录 schema/迁移、文件布局、缓存失效、保留期、删除语义和恢复方式；不存在则删除本节。执行数据写入、迁移、清理或回滚前仍需明确授权。

## 5. 完整性记录

- 关键对象：{发现数}/{已文档化数}/{排除数}。
- 状态机与枚举：{发现数}/{已文档化数}/{排除数}。
- 验证基线：{branch@commit + 时间}。
