# 数据模型

> 状态：草案（待人工确认）

## 1. 数据库设计规范
- 表名前缀：{待填，如 `tulingshop_` 风格的项目统一前缀}。
- 必备字段：`id`、创建时间、更新时间、逻辑删除标记（如适用）。
- 金额等精度敏感字段按 `CLAUDE.md` 红线处理。

## 2. 核心表结构

### 2.1 {表一}
| 字段 | 类型 | 说明 | 约束 |
|------|------|------|------|
| id | {类型} | 主键 | PK |
| {待填} | | | |

### 2.2 {表二}
（同上结构）

## 3. 缓存数据结构设计
| Key 模式 | 类型 | TTL | 内容 | 更新策略 |
|----------|------|-----|------|----------|
| `{前缀}:{模块}:{业务}:{标识}` | {待填} | {待填} | {待填} | 先写库后删缓存 |

## 4. 消息队列结构（如适用）
| Topic/队列 | 生产者 | 消费者 | 幂等键 | 说明 |
|-----------|--------|--------|--------|------|
| `{前缀}-{模块}-{事件}` | {待填} | {待填} | {待填} | |

> 维护：新表/新 Key/新 Topic 由 harness-sync 反补登记。
