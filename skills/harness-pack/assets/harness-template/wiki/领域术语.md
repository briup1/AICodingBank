# 项目术语表

> 状态：草案（待人工确认）
> 定位：消除产品语言、UI 文案、代码、协议和外部系统之间的**同义词**——AI 给新概念命名、翻译需求名词时先查本文，禁止自造同义词。
> 激活入口：CLAUDE.md 场景触发行（术语→本文）；`skills/编码实现/SKILL.md` Step 3 命名检查点。
> 验证基线：{branch@commit + 时间}
> 纪律：只收录影响需求理解、接口、状态或代码定位的词；**不收通用软件术语的定义**（DTO/VO/聚合根之类 AI 默认就懂），只收本项目的实际命名模式（见第 4 节）；不因术语表存在而强制全局改名。

## 1. 核心术语

| 推荐术语 | 产品/UI 名称 | 代码名称 | 协议/存储名称 | 准确定义 | 不等同于 | 适用范围 | 来源 | 状态 |
|----------|--------------|----------|---------------|----------|----------|----------|------|------|
| {按项目填写} | {文案或 N/A} | {类型/变量} | {wire/schema 或 N/A} | {一句可判定定义} | {易混淆概念} | {模块/流程} | {路径/需求} | proposed/current/deprecated |

## 2. 同义词、历史名与外部术语

| 名称 | 对应推荐术语 | 出现场景 | 是否继续使用 | 迁移/兼容说明 | 来源 |
|------|--------------|----------|--------------|---------------|------|
| {按项目填写} | {术语} | {历史代码/外部协议/UI} | 是/否/仅兼容 | {说明} | {路径} |

## 3. 容易混淆的概念

### {概念 A} 与 {概念 B}

- `{概念 A}`：{定义、所有者和生命周期}。
- `{概念 B}`：{定义、所有者和生命周期}。
- 判断方法：{开发或排障时如何区分}。
- 相关代码/契约：{路径或接口 ID}。

## 4. 命名约定（项目立法，非通用风格）

从代码命名归纳（legacy）或用户立法（greenfield），给新字段/类型/事件命名时照抄，不猜测：

- **单位**：{如金额一律 `{属性}Cents` 存储，禁止裸 float 金额}
- **布尔**：{如 `is`/`has` 前缀}
- **时间**：{如 `Time` 后缀与时区约定}
- **ID**：{如 `Id` 后缀}
- **命令/事件/请求响应**：{如 `XxxCmd`/`XxxVo`/`XxxResponse`——只记本项目实际在用的模式}

## 5. 状态与枚举导航

状态值、允许迁移和未知值处理统一记录在 `wiki/数据模型.md` 第 3 节；本节只保留术语到对应状态对象的链接，**不复制枚举定义**。

## 6. 维护

- 新增术语时同时核对需求、UI、代码和 wire 名称。
- 外部协议固定名称可与产品词并存；通过映射消歧，不为表面一致进行无收益重命名。
- 术语含义变化若影响接口或状态，同时更新对应 Wiki 和测试。
