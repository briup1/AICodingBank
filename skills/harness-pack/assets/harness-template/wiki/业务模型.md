# 项目能力地图

> 状态：草案（待人工确认）
> 定位：需求的落点地图（一句话需求 → 能力 → 代码入口）+ 流程模式的剧本库（项目既有流程模式防止 AI 现场发明）。
> 激活入口：CLAUDE.md 场景触发行（流程→本文）；`skills/需求分析/SKILL.md` Step 2。
> 验证基线：{branch@commit + 时间}
> 目标：从用户或调用方能力导航到代码、接口、状态和验证。分组按项目实际结构归纳，**不强制 DDD 域划分**——只有项目明确采用领域模型时才改用"领域"语义。

## 1. 参与者与系统边界

| 参与者/外部系统 | 目标或职责 | 进入方式 | 信任边界 | 来源 |
|-----------------|------------|----------|----------|------|
| {按项目填写} | {按项目填写} | {UI/API/CLI/Event} | {权限/身份/网络} | {需求/代码} |

## 2. 能力地图

| 能力 | 用户/调用入口 | 调用方实现 | 提供方实现 | 关键逻辑/模式 | 接口/事件 | 状态所有者 | 验证 | 状态 |
|------|---------------|------------|------------|---------------|-----------|------------|------|------|
| {核心能力逐项填写} | {页面/命令/API} | {路径} | {路径/外部系统} | {一句话模式短语，如：预扣库存——下单锁定、支付确认、超时释放} | {契约 ID} | {模块/系统} | {测试/evidence} | proposed/current/deprecated |

能力必须覆盖实际用户入口或公开调用入口；导航分组不自动等同于限界上下文、事务边界、所有权或发布边界。

## 3. 核心流程

每条核心能力至少给出一条主流程；高风险或常见失败路径单独展开。时序用线性文本（触发 → 入口 → 处理 → 状态 → 结果），不画图形 UML。

### {流程名称}

**触发与完成条件**

- 触发：{用户动作/调用/事件}。
- 完成：{可观察结果}。

```text
{参与者}
  → {入口模块与动作（带路径）}
  → {接口/消息（契约 ID）}
  → {处理模块（带路径）}
  → {状态或外部系统}
  → {响应/通知}
  → {最终结果}
```

**状态变化**

| 步骤 | 所有者 | 变化前 | 事件/动作 | 变化后 |
|------|--------|--------|-----------|--------|
| {按项目填写} | {模块/系统} | {状态} | {动作} | {状态} |

状态枚举全集与合法迁移以 `wiki/数据模型.md` 第 3 节为准，此处不复述。

**错误路径与不变量**

- {失败如何呈现、是否重试、如何清理——如：回调重复到达走幂等、延迟检查到达时已支付则跳过}。
- {不能被破坏的业务/技术约束}。

**实现与验证**

- 实现：{路径/符号}。
- 契约：{接口 ID/文档}。
- 验证：{测试/evidence}。

## 4. 能力完整性

- 入口发现数：{数量}；已映射能力：{数量}；排除：{数量与理由}。
- 每项 current 能力均连接代码、契约、状态和验证；缺失项标为技术债或待确认，不以空链接代替。
- 验证基线：{branch@commit + 时间}。
