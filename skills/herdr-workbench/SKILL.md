---
name: herdr-workbench
description: 在 Herdr 中进行开发、拆分子 Agent、并行实现或审查时应用个人编排策略。用户无需重复指定：只要当前会话位于 Herdr 且开发任务适合委派，就用上游 herdr Skill 控制 pane 和 Agent，并让子 Agent 以无人工权限确认的 YOLO 模式运行。
---

# Herdr Workbench

本 Skill 只定义个人编排策略。Herdr 的 workspace、tab、pane、Agent 生命周期和 CLI 语法以上游 `herdr` Skill 及当前 `herdr --help` 为准，不在这里复制。

## 进入条件

```text
开发任务
  -> 检查 HERDR_ENV=1
  -> 任务是否存在边界清楚、可独立验收的子任务？
       ├─ 否：当前 Agent 直接完成
       └─ 是：使用上游 herdr Skill 委派
```

- 用户明确提到 Herdr 时总是检查本策略。
- 用户未提 Herdr，但当前处于 Herdr 会话且开发任务适合委派时，自动应用本策略。
- `HERDR_ENV` 不是 `1` 时不控制 Herdr；当前 Agent 本地完成，不要求用户重复提示。
- 不为很小的任务、当前步骤的即时阻塞项或写集合重叠的任务创建子 Agent。

## 拆分规则

子任务必须同时满足：

1. 有一个清晰目标和可量化完成标准。
2. 明确允许读取和修改的路径。
3. 能独立运行聚焦验证。
4. 与并行任务不存在共享写集合。
5. 主 Agent 不依赖其即时结果才能继续当前步骤。

优先按垂直结果拆分，例如“实现并验证一个完整行为”；不要按模型、控制器、测试等水平层拆分。主 Agent 始终负责上下文、排序、冲突处理、集成和最终验证。

## 子 Agent 默认 YOLO

所有通过 Herdr 新启动的编码子 Agent 默认使用该 Agent 当前 CLI 支持的无人工权限确认模式。启动前运行对应 `<agent> --help` 核验参数，不凭记忆猜测。

当前已验证映射见 `references/compatibility.md`。原生参数必须放在 `herdr agent start ... --` 之后。

YOLO 只用于用户已经批准的目标项目或隔离 worktree 内的开发任务。子 Agent 提示必须限制写入范围，并排除：

- 生产环境、发布和外部系统写操作
- Token、密码、Cookie、私钥和客户敏感数据
- 未经授权的 Git push、破坏性 Git 操作和目录删除
- 超出任务边界的配置、依赖或权限修改

命中这些范围时不以 YOLO 委派；由主 Agent 停下并取得明确授权。

## 委派与回收

按上游 `herdr` Skill 完成布局检查、pane 创建、Agent 启动、prompt、wait 和 read。默认使用当前 tab 的同级 pane、保持当前工作目录且不抢用户焦点；只有隔离确有价值时才创建 worktree。

每个子 Agent 的提示至少包含：

- 目标和非目标
- 允许读写路径
- 现有架构或规范入口
- 完成标准和验证命令
- 禁止的高风险操作
- 输出：改动文件、执行命令、结果、假设和剩余风险

`blocked` 不视为完成。出现 blocked、unknown、超时或启动失败时，读取状态和最近输出；只有确需用户决策或高风险授权时才打断用户。子 Agent 的完成声明不是证据，主 Agent 必须在集成后重新运行相关验证。

## 完成标准

- 每个委派任务都有独立产物或结论及验证输出。
- 并行任务没有未处理的写冲突。
- 主 Agent 已检查完整 diff，并重新运行最终测试或审查。
- 没有因 YOLO 绕过生产、凭证、发布或破坏性操作的授权边界。
