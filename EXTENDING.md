# 拓展个人 AI 工作台

本文件是工作台的**拓展入口**。当用户要新增、纠正或长期保留规则、知识、技能、偏好、项目入口或工作流时，Agent 先读本文件，自行判断信息类型并写入唯一事实来源；不要要求用户先决定文件位置。

## 拓展流程

```text
理解用户希望长期改变什么
  -> 判断作用范围与信息类型
  -> 查找现有事实来源和相近能力
  -> 优先扩展现有内容
  -> 写入唯一归宿
  -> 执行对应验证
  -> 告知用户以后如何自动生效
```

若用户同时要求完成当前项目任务和沉淀经验，先按 [WORKBENCH.md](WORKBENCH.md) 完成并验证任务，再把已经验证、值得复用的信息按本文件归档。未经验证的临时判断不进入长期知识。

## 自动分类与唯一归宿

```text
新增或更新的信息
  |
  +-- 启动时必须执行的意图路由
  |     -> AGENTS.md 与 CLAUDE.md（保持语义一致、内容极短）
  |
  +-- 稳定个人偏好、本机路径、私人项目索引
  |     -> WORKBENCH.local.md
  |
  +-- 可共享的工作台用法或拓展规则
  |     -> WORKBENCH.md 或 EXTENDING.md
  |
  +-- 条件触发的操作规则、完整步骤或工具能力
  |     -> 自研 Skill：skills/ + skills.yaml
  |
  +-- 已存在的第三方 Skill
  |     -> skills-lock.json；由 install.py 管理 vendor/
  |
  +-- 单个项目的代码、命令、接口、架构或开发规范
  |     -> 对应项目仓库的 AGENTS.md、README 或项目文档
  |
  +-- 已验证且跨会话复用的知识、经验、踩坑
  |     -> Knowledge Base
  |
  +-- 需求、方案、验收证据或任务状态
        -> 目标项目文档或 Pi Goal，不写入工作台规则
```

无法归类时依次判断：

1. **作用范围**：仅本机、当前工作台、单个项目，还是跨项目。
2. **用途**：Agent 必须遵守的行为，还是按需查询的事实。
3. **触发方式**：每次启动都需要，还是仅在特定任务中需要。
4. **生命周期**：稳定长期、项目生命周期，还是一次性临时信息。

只有分类结果会改变实际行为或暴露敏感信息时才向用户澄清；否则选择最窄、最接近现有事实来源的位置。

## 各类拓展方式

### 1. 个人偏好与本机上下文

写入 Git 忽略的 `WORKBENCH.local.md`，适合：

- 回复语言和输出习惯
- 稳定的开发偏好
- 本机工作区路径
- 私人项目索引和知识入口
- 不适合共享到仓库的个人约束

同步更新 `WORKBENCH.local.example.md` 的字段结构，但不要把真实私人内容复制到示例。

以下内容不得写入：Token、密码、Cookie、私钥、完整连接串、客户敏感数据、会话原文和临时任务进度。

### 2. Agent 行为规则与工作流

符合以下任一条件时优先做成 Skill，而不是继续扩充 `AGENTS.md`：

- 只在某种工具、语言、阶段或任务中触发
- 包含多步操作、参数选择、检查或失败处理
- 需要独立维护操作手册
- 希望所有已安装该 Skill 的 Agent 自动发现

先搜索 `skills/`、`skills-lock.json` 和 `CATALOG.md`，存在相近能力时扩展原能力。自研 Skill 放在 `skills/<skill-name>/`，并在 `skills.yaml` 的 `self` 下登记：

```yaml
- name: <skill-name>
  path: skills/<skill-name>
  capability: <它能做什么>
  boundary: <它不做什么、依赖什么>
  tags:
  - <扁平标签>
  verified: ''
```

开发与修改 Skill 时遵守 Skill 自身的编写规范。`verified` 为空时不会安装；真实场景验证通过后填写日期，再运行：

```bash
python3 install.py
```

安装器是幂等的，并会刷新 `CATALOG.md`。不要手改 `CATALOG.md`。

### 3. 第三方 Skill

只在 `skills-lock.json` 登记上游，不复制文件进仓库。每条登记必须包含：

- `capability`：能做什么
- `boundary`：不做什么、外部依赖和适用边界
- `tags`：扁平标签
- `verified`：真实验证日期；未验证时留空

运行 `python3 install.py` 后，安装器才可从上游管理 `vendor/` 并安装已验证 Skill。`vendor/` 是缓存，不是编辑入口。

需要个性化第三方 Skill 时，保留上游 Skill 不动，另建自研策略 Skill 组合调用；不要 fork 或修改 `vendor/`。固定 `ref` 的已验证 Skill 必须保存入口文件 `computedHash`，安装时哈希不符会移除旧链接并拒绝安装。跟随默认分支的历史登记暂不强制哈希，后续应逐步迁移到固定版本。

升级第三方 Skill：发现候选版本 → 阅读上游变化 → 修改 `ref` 并清空 `verified` → 运行安装器移除旧链接 → 更新 `computedHash` → 完成兼容验证 → 填写新验证日期 → 重新安装。定时检查只能产生候选报告，不自动启用新版本。

当前机器临时停用已验证 Skill 时，写入 Git 忽略的 `skills.local.yaml`，然后重新运行安装器；不要篡改共享验证状态。

### 4. 项目规则与项目事实

仅对某个项目有效的信息必须留在目标项目中。进入目标项目后读取其 `AGENTS.md`、`README`、构建配置和现有文档，再扩展最接近的事实来源。

不要把以下内容复制到工作台：

- 项目架构和接口清单
- 可以从构建配置直接得出的命令
- 临时分支、任务进度和调试状态
- 目标项目已经维护的规范全文

`WORKBENCH.local.md` 的项目索引只保存名称、绝对路径、一句话稳定用途和入口依据。

### 5. 知识与经验

已验证且值得跨会话复用的信息进入 Knowledge Base。写入前必须读取知识库 `_index.md` 和 `_schema.md`，按 schema 选择目录并遵守 frontmatter、wikilink、tags 和表格规则；写入后运行知识库审计并修复问题。

Skill 本体不进入知识库。知识库只记录 Skill 的使用经验、评测、限制和踩坑。

### 6. 机器配置

机器上的真实配置应写入工具规定的配置文件，工作台只记录“应使用什么”和获取方法，不复制配置状态。

例如 Python 开发的稳定偏好可以写入 `WORKBENCH.local.md`，具体 uv 镜像配置写入：

```toml
# ~/.config/uv/uv.toml
[[index]]
url = "https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple"
default = true
```

涉及版本敏感的工具参数时，先读取当前 CLI 的 `--help` 或官方文档，不把可能过期的参数散落在多个文件。

## 入口文件维护原则

`AGENTS.md` 和 `CLAUDE.md` 只承担意图路由。只有“每次进入仓库都必须知道”的内容才可进入；条件规则通过一句明确指针指向本文件或 Skill。

每次拓展都检查：

- **单一事实来源**：同一规则只维护一次，其他位置只放指针。
- **渐进披露**：入口短，具体内容按意图和触发条件加载。
- **扩展优先**：优先修改现有文件或 Skill，不创建功能重叠的新入口。
- **可验证**：行为规则要有触发示例或最小检查；Skill 和安装登记需运行安装器验证。无法满足真实运行前提时保持 `verified` 为空，不以静态检查冒充可用性验证。
- **安全边界**：高权限、凭证、部署、删除和外部写操作必须保留明确授权边界。

## 完成标准

一次拓展只有同时满足以下条件才算完成：

1. 信息进入了唯一且作用范围正确的事实来源。
2. 入口或 Skill 描述包含能够自动命中的触发条件。
3. 没有在工作台、项目和知识库之间复制同一正文。
4. 对应格式、安装、测试或知识库审计已经通过。
5. Agent 下次仅凭用户意图即可加载该信息，无需用户再次说明存放位置。
