# 个人 AI 工作台

本文件是工作台的**使用入口**。当用户在本目录提出了解、规划、开发、调试、测试、审查、部署或沉淀当前工作的请求时，Agent 按本文件自动定位项目、判断阶段并获取所需能力；用户无需说明该使用哪个 Skill 或文件。

用户要新增、纠正或长期保留规则、知识、Skill、偏好、项目入口或工作流时，转到 [EXTENDING.md](EXTENDING.md)。如果两种意图同时出现，先完成并验证当前工作，再沉淀已经验证的信息。

## 工作台能提供什么

| 用户意图 | 工作台提供的帮助 | 获取位置 |
| --- | --- | --- |
| 找到并进入项目 | 从本机项目索引定位真实仓库，再读取项目入口文件 | `WORKBENCH.local.md` + 目标项目 |
| 理解现状或调查资料 | 选择观察阶段，读取项目、知识库或可信外部来源 | 阶段路由 + 对应 Skill |
| 确认需求、设计方案 | 根据当前证据进入确认或设计阶段 | 阶段路由 + 开发流程 Skill |
| 编码、修复、测试、审查、部署 | 在目标项目中选择最小能力组合并留下验证证据 | `CATALOG.md` / 已安装 Skill |
| 管理长任务 | 使用 Pi Goal / Personal Workbench 保存状态和产物 | Goal 工具与任务 DAG |
| 查询长期知识和经验 | 按知识索引读取，不在工作台复制外部事实 | Knowledge Base |
| 增加工作台能力 | 自动分流到拓展入口 | `EXTENDING.md` |

## 系统地图

```text
                        人：目标、取舍、批准
                                  |
                                  v
                         WORKBENCH.md
                    定位、阶段路由、组件职责
                                  |
          +-----------------------+-----------------------+
          |                       |                       |
          v                       v                       v
  WORKBENCH.local.md       AICodingBank              项目真实目录
  偏好、项目指针            Skill 能力控制面           代码、测试、文档
          |                       |                       |
          +-----------------------+-----------------------+
                                  |
                   +--------------+--------------+
                   |                             |
                   v                             v
             Pi Goal / DAG                 Knowledge Base
             持续执行与状态                 长期知识与经验
```

组件职责：

- **当前仓库**：工作台入口，也是 Skill 的能力控制面。
- **`WORKBENCH.local.md`**：本机个人上下文和项目索引；被 Git 忽略。
- **项目仓库**：代码、接口、命令和运行行为的事实来源。
- **Pi Goal / Personal Workbench**：长任务执行、状态和产物可观测性。
- **Knowledge Base**：项目档案、调研和已验证经验；按需读取。

## 开始一次工作

```text
读 WORKBENCH.md
  -> 若存在，读 WORKBENCH.local.md
  -> 根据用户指定路径或项目索引定位项目
  -> 读取项目自己的 AGENTS / README / 构建配置
  -> 判断唯一当前阶段
  -> 按需加载对应 Skill
  -> 在项目真实目录中工作并留下验证证据
```

路径、项目说明或文档互相冲突时，以用户当前要求和项目真实文件为准；本地索引只负责导航。

## 阶段路由

以用户本次希望得到的结果为主，以已有证据为辅。一次请求只选择一个当前阶段；完成后再决定是否进入下一阶段。

```text
理解事实？ --------> Observe / 观察
确认目标？ --------> Confirm / 确认
决定做法？ --------> Design / 设计
产生改动？ --------> Execute / 执行
证明正确？ --------> Verify / 验证
保存经验？ --------> Learn / 沉淀
```

| 阶段 | 何时进入 | 完成证据 | 优先复用 |
| --- | --- | --- | --- |
| **Observe / 观察** | 用户只想了解项目、现象、资料或历史，不要求持久变更 | 基于真实文件、命令或可信来源的事实结论 | 项目阅读、调研、诊断能力 |
| **Confirm / 确认** | 新需求或行为变化的目标、边界、价值、验收尚未确认 | 已批准需求，或清晰且可验收的小任务边界 | `wl-req-confirm`、`wl-brainstorming` |
| **Design / 设计** | 目标已稳定，但接口、复用、风险或实施路径仍未确定 | 已批准方案；简单任务则为用户批准的最小方法 | `wl-design-doc`、`wl-writing-plans`、方案审查类 Skill |
| **Execute / 执行** | 用户要求产生改动，且目标与修改边界已经稳定 | 实际产物、命令结果和范围证据 | `wl-ticket-run`、`wl-subagent-driven-development`、`wl-investigate`、Pi Goal |
| **Verify / 验证** | 已有实现或产物，需要判断正确性、质量或是否可交付 | 实际测试、审查或验收结果 | `wl-requesting-code-review`、测试与浏览器验证能力 |
| **Learn / 沉淀** | 已验证结论具有跨会话或跨项目复用价值 | 写入适当的 Memory、知识库或项目文档 | Memory 与知识库流程 |

判断冲突时：

```text
用户明确指定阶段
  -> 已批准文档或既有任务状态
  -> 用户本次希望得到的结果
  -> 仍无法唯一判断时，只问一个会改变阶段的澄清问题
```

阶段不是必须走满的流水线：

- 只读问题可在 Observe 结束。
- 范围明确的简单操作可直接进入 Execute，但写代码前仍说明最小方法并获得批准。
- Bug 修复属于 Execute，内部先复现、再修复、再验证。
- 提交、部署等操作可直接进入 Execute，但必须遵守授权和安全边界。
- 没有实际证据，不得宣称阶段完成。

## 能力控制面

AICodingBank 继续负责“能力能否被发现、安装和信任”：

- 自研 Skill：`skills/` + `skills.yaml`
- 第三方 Skill：`skills-lock.json` + `vendor/`
- 按需 Skill：`skills-on-demand/`
- 人类目录：`CATALOG.md`
- 安装入口：`python3 install.py`
- 本机停用：`skills.local.yaml`

本文件只做阶段路由，不复制 Skill 的详细说明。进入阶段后，从 `CATALOG.md` 或当前 Agent 的可用 Skill 中选择最小能力组合。

## 个人上下文与项目索引

复制 `WORKBENCH.local.example.md` 为 `WORKBENCH.local.md` 后维护本机信息。共享文件只定义格式，真实内容不进入 Git。

项目索引只保存：

- 项目名称与本地路径
- 一句话稳定用途
- 对应知识入口
- 项目内真实工作入口

不要在索引里复制项目架构、脚本清单或临时任务进度；这些内容会过期，应在进入项目后读取真实文件。

## 信息归宿

| 信息 | 保存位置 |
| --- | --- |
| 稳定个人偏好、私人项目路径 | `WORKBENCH.local.md` |
| Skill 能力、来源、边界、验证状态 | `skills.yaml` / `skills-lock.json` |
| 项目代码、命令、接口和架构 | 对应项目仓库 |
| 需求、方案和验收证据 | 对应项目的需求文档 |
| 长任务状态和产物 | Pi Goal / Personal Workbench |
| 跨会话长期知识 | Knowledge Base / Memory |
| 一次性探索和临时进度 | 当前会话或项目临时目录，不进入个人上下文 |

## 边界

- 人负责目标、取舍、方案批准和高风险授权；Agent 负责调查、实施和提供证据。
- 不把项目迁入本仓库，不统一各项目 Git 边界。
- 不在工作台文件中保存凭证、密钥、Cookie、客户数据或完整会话原文。
- 不为工作台另建数据库、服务或流程引擎，除非文档骨架经过实际使用后证明不足。
