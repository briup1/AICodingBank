你是一位经验丰富的系统架构师，请基于以下两份输入材料，为本次二次开发任务撰写一份**精炼、可执行、面向落地的技术设计文档**：

- **【项目最新现状】**：`@.clinerules/current_project_manual/UExtract功能概要_2025-12-18.md`  
- **【产品需求文档（PRD）】**：`@.clinerules/需求/新增字段管理和信息抽取功能PRD.md`

---

### 🎯 核心目标

本次是**成熟系统的增量开发**，技术方案必须优先考虑**与现有架构的融合性**。请重点完成以下任务：

1. **识别并明确指出可复用的现有资产**（如已有模块、服务、工具类、配置模板、数据结构、API 等），并在文档中提供**具体路径或代码位置线索**（例如：“可复用 `common/extractor/base.py` 中的 `FieldExtractor` 基类”）。
2. 所有复用建议必须基于对项目现状的合理分析与推断，**确保资产真实存在或经小幅改造即可复用**，避免假设性描述。
3. 新功能应以**最小侵入方式**嵌入系统，优先扩展而非重写。

---

### 📄 输出要求

- **输出格式**：Markdown
- **保存路径**：`/docs/design/{需求简称}.design.md`（请根据 PRD 内容提取简洁的需求简称，如 `field_extraction`）
- **文档结构**：请严格按需裁剪以下模板，**仅保留与本次需求强相关的章节**，删除无关部分以保持精炼：

  1. **项目背景与目标**（简述本次开发在整体系统中的定位）
  2. **关键功能需求摘要**（从 PRD 中提炼，1–3 条核心点）
  3. **系统集成策略**（重点：如何嵌入现有架构？复用哪些组件？改动边界在哪？）
  4. **数据模型变更**（如有新增/修改字段、表结构等）
  5. **API 接口设计**（仅列出新增或变更的接口）
  6. **关键技术实现要点**（含复用线索 + 扩展逻辑）
  7. **部署与配置影响**（是否需要新配置项、环境变量、依赖升级？）
  8. **风险与应对措施**（如兼容性、性能瓶颈、回滚方案）
  9. **扩展性说明**（为未来类似需求预留的设计考虑）

> ⚠️ 注意：**不要包含 UI 设计规范**（除非 PRD 明确要求前端交互变更），本项目为后端/引擎层功能增强。

---

### ✨ 风格要求

- 语言专业、简洁，避免冗余描述
- 每个技术决策需附带**理由或依据**（如“复用 X 模块因其已支持 Y 能力，符合本次 Z 需求”）
- 使用**具体路径、类名、函数名、配置键**等可追溯标识，便于开发直接定位

---

请开始分析输入材料，并生成符合上述要求的技术设计文档。
