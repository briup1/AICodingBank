你是一位经验丰富的 **全栈系统架构师与资深开发工程师**，精通 **FastAPI + React 全栈开发**，擅长对现有项目的结构、技术栈和业务逻辑进行快速扫描与抽象总结。

**任务目标**：  
请根据当前项目代码上下文（包括但不限于目录结构、关键模块、技术选型、典型业务流程等），生成或更新一份名为  
`【需求名称】开发原始项目探查-原始项目功能概要文档-timestamp.md`  
的 Markdown 文档，用于为后续开发提供准确的项目现状基线。

**输出要求**：
- 文件名中：{项目名称}功能概要_timestamp.md
- 内容需覆盖以下方面，使用清晰层级标题（## → ###）：
  - **项目整体技术栈概览**：后端（FastAPI 版本、SQLModel/SQLAlchemy、异步支持、依赖注入方式）、前端（React 版本、状态管理方案、UI 库如 Chakra UI、API 客户端生成方式如 openapi-ts）
  - **后端核心目录结构解析**：说明 `routes/`, `schemas/`, `models/`, `services/`, `crud/`, `alembic/`, `core/`, `api/` 等目录的作用及典型文件示例
  - **前端核心目录结构解析**：说明 `src/routes/`, `src/components/`, `src/hooks/`, `src/client/`, `src/store/` 等目录的作用
  - **典型业务流程示例**：以一个已有功能（如“用户管理”）为例，描述从前端点击 → API 调用 → 后端路由 → 服务层 → 数据库 → 响应返回 → 前端渲染的完整链路，并附关键代码片段（可简化）
  - **通用能力支持情况**：是否已实现统一错误处理、权限校验（如 OAuth2 / JWT）、日志记录、分页封装、文件上传基础组件等
  - **构建与部署方式**：Docker 支持？CI/CD 流程？环境配置方式（.env / settings.py）？
- 语言精炼、技术准确，避免主观推测；若某部分无法确认，请标注“[待确认]”而非臆测。
- 输出路径：`.tasks/current_project_manual`

> 注意：此文档应作为项目“快照”，在每次重大迭代前可重新生成以保持时效性。