---
illustration_id: 03
type: flowchart
style: sketch-notes
palette: macaron
---

gstack 审查流水线 - 手绘流程图

Hand-drawn educational flowchart on warm cream paper. Black hand-drawn lines
with slight wobble, soft pastel color blocks. Simple, friendly, diagram-style
visuals ONLY.

PALETTE: macaron — soft pastel color blocks
COLORS: Warm Cream background (#F5F0E8); Black (#1A1A1A) for ALL lines, text,
        arrows, and doodles; Macaron Blue (#A8D8EA), Lavender (#D5C6E0),
        Mint (#B5E5CF), Peach (#FFD5C2); Coral Red (#E8656D) for emphasis
        and final ship step.
        Color values and names are rendering guidance only — do NOT display
        color names, hex codes, or palette labels as visible text.

LAYOUT: Horizontal left-to-right pipeline, with one optional parallel branch.

STEPS:
1. [Rounded card] "/plan-ceo-review" — lightbulb icon, label "战略审查"
2. [Diverging arrow] splits into two parallel cards below the main line:
   2a. "/plan-eng-review" — gear icon, label "工程审查"
   2b. "/plan-design-review" — palette icon, label "设计审查"
3. [Merging arrow] back to main line → "/plan-devex-review" — terminal icon,
   label "开发者体验"
4. "/autoplan" — robot/automation icon, label "自动审查"
5. "/review" — magnifying-glass icon, label "代码审查"
6. "/ship" — package/rocket icon, label "发布", Coral Red outline for emphasis

CONNECTIONS: Thick wavy hand-drawn arrows between each step. The parallel branch
             uses a "Y" split and merge. Small Chinese annotations like
             "并行" near the split. Dashed arrow from "/autoplan" suggesting
             it can run all prior reviews automatically.

ELEMENTS: Rounded cards with dashed/solid borders, wavy hand-drawn arrows with
          labels, simple icons per step, small doodle decorations (stars,
          underlines). Clean composition with generous white space.

STYLE: Color fills don't completely fill outlines, hand-drawn lettering, generous
       white space. ALL text hand-lettered in Chinese — no computer fonts.

TEXT: Large and prominent with handwritten-style fonts. Short labels only.
      All text in Chinese.

ASPECT: 16:9
COMPLEXITY: medium
